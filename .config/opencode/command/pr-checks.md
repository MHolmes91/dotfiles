---
description: Inspect pull request checks and summarize failures.
agent: gh-ops
---
Load `gh-pr-issue` and apply the "View Pull Request" instructions (JSON mode) for the given PR number.
Summarize each failing check with its name, status, and URL.
