---
description: Monitor a workflow run using gh run watch.
agent: gh-ops
subtask: true
---
Load `gh-workflows` and follow the "Monitor a Run" section using `gh run watch $1 --exit-status`.
Stream the logs and summarize whether the run passed, failed, or was cancelled.
